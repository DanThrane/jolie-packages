profile "default" configures "registry" {
    ENABLE_KILL_COMMAND = false,
    KILL_TOKEN = "",
    FRESH_TOKEN = 60000L,
    DATA_DIR = "data",
    TRUSTED_PEERS = {
        // No trusted peers by default
        .location[0] = ""
    },
    PUBLIC_LOCATION = "socket://localhost:12345"
}

